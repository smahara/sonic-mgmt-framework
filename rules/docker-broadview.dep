
DPATH       := $($(DOCKER_BROADVIEW)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-broadview.mk rules/docker-broadview.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_BROADVIEW)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_BROADVIEW)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_BROADVIEW)_DEP_FILES   := $(DEP_FILES)

