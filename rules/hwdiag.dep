
SPATH       := $($(HWDIAG)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/hwdiag.mk rules/hwdiag.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(HWDIAG)_CACHE_MODE  := GIT_CONTENT_SHA 
$(HWDIAG)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(HWDIAG)_DEP_FILES   := $(DEP_FILES)

