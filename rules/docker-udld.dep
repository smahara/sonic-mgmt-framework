
DPATH       := $($(DOCKER_UDLD)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-udld.mk rules/docker-udld.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_UDLD)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_UDLD)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_UDLD)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_UDLD),$(DOCKER_UDLD_DBG)))
