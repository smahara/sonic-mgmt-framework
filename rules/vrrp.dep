
SPATH       := $($(VRRP_PKG)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/vrrp.mk rules/vrrp.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(VRRP_PKG)_CACHE_MODE  := GIT_CONTENT_SHA 
$(VRRP_PKG)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(VRRP_PKG)_DEP_FILES   := $(DEP_FILES)

