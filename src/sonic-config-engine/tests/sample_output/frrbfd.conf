!
! =========== Managed by sonic-cfggen DO NOT edit manually! ====================
! generated by templates/frr/frr.conf.j2 with config DB data
! file: frr.conf
!
!
!
password zebra
enable password zebra
!
log syslog informational
log facility local4
!!
agentx
!
!
!
! Enable link-detect (default disabled)
!
! Set ip source to loopback for bgp learned routes
ip protocol bgp route-map RM_SET_SRC
!
!!
!
! set static default route to mgmt gateway as a backup to learned default
!!
!
!!
bfd
  peer  peer 2.2.2.2 interface Ethernet0 vrf default 
    transmit-interval 400 
    detect-multiplier 5
    echo-mode
    echo-interval 44
    shutdown
  !
  peer  peer 5.5.5.5 interface Ethernet0 vrf 1.1.1.1 
    shutdown
  !
!
bfd
  peer  peer 10.1.1.1 vrf default local-address 10.1.1.2 multihop
    transmit-interval 200
    receive-interval 250
    no shutdown
  !
!

